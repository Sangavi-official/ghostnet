# ── Security (NEVER commit these) ──────────────────────────
*.pem
*.key
*.p12
*.pfx
.env
.env.*
secrets.py
credentials.json

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.Python
pip-log.txt

# ── Virtual environments ─────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── Model checkpoints (large binaries — keep only final) ────
# Uncomment below if you want to exclude ALL zips:
# *.zip
# Keep these tracked (final trained models):
# ghostnet_final.zip
# best_model/best_model.zip

# ── Logs & eval artifacts ───────────────────────────────────
logs/
*.npz
*.csv

# ── OS junk ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ─────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

*.pem
mutation_ledger.json
mutation_ledger.json.tmp
dashboard/events.jsonl
ledger_selftest.json
__pycache__/
